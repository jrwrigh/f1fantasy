def create_constructors(driverdict):
    constructordict
    for drvier in drivertable:
        if driver.team not in construcdict.keys():
            drivers = []
            for driver in drivertable.filter[driver.team].iterrows():
                drivers.append(driver['Name'])
            f1fantasy.constructor(driver.team, drivers)

            